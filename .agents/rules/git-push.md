# Git Auto-Push Rule

Whenever files are modified, created, or fixed in this repository:
1. Ensure all changes are cleanly staged.
2. Commit with a meaningful conventional commit message.
3. Automatically push the changes to GitHub (`git push origin main`).
